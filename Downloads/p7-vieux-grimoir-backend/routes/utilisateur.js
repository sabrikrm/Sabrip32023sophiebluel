const express = require('express');
const router = express.Router();
const Utilisateur = require('./models/utilisateur');

// creér un utilisateur
router.post('/utilisateur', (req, res, next) => {
    // La logique
  });

  //se connecter compte deja existant